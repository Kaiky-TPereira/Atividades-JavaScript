function mediaArit(soma, quantidade){
    var media = soma / quantidade;
    alert(media);
}

var valores = [1,2,3,4,5];
var soma = valores[0] + valores[1] + valores[2] + valores[3]  + valores[4]
var quantidade = valores.length;

mediaArit(soma,quantidade);